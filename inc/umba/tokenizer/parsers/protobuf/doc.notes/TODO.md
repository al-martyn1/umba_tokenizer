# TODO для protobuf

- [ ] Нужно сделать ContextDependedIdentifierToKeywordConversionFilter, аналогично IdentifierToKeywordConversionFilter,
      context - это что-то типа enum, по нему переключаются наборы обрабатываемых ключевых слов.
      Значение контекста передаём как shared_ptr.

- [ ] Нужно сделать фичи. Фичи могут быть:
  - bool
  - int
  - unsigned
  - enum - строки из определенного набора
  - строки

- [ ] Фичи делаем как variant

- [ ] Lifecycle of a Feature:
  - active
  - deprecated
  - removed

- [ ] Метаинформация о фичах:
  - kind фичи
  - lifecycleLevel
  - default value
  - для enum-фич - список допустимых значений

- [ ] Фичи храним в виде дерева. Надо сделать контейнер для фич, который парсит ключи на составные части и искать в дереве.
      Поиск по FQN. В узлах дерева храним либо значение фичи, либо поддерево.

- [ ] FQN - хранить ли признак независимости от регистра прямо в FQN? Или, в случае дерева фич, храним признак независимости от регистра в контейнере фич?

- [ ] Сделать новую репу с тестами для umba-protoc - umba-protoc-dev-tests


